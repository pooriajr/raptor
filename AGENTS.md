# AGENTS.md

This repository keeps AI agent guidance in `CLAUDE.md`.

If your tool only looks for `AGENTS.md`, use this file as the entry point
and then read `CLAUDE.md` for the canonical instructions and project context.

When updating agent guidance, prefer editing `CLAUDE.md` and keep this file
in sync with any high-level changes.
